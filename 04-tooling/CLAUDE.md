# CLAUDE.md — 04-tooling

- Module: 04-tooling
- Purpose: Bash tool, slash commands, and MCP exercises
- Available tools: Bash, Read, Write
- Key concepts:
  - read→act→verify loop
  - permission prompts as the user control point
  - slash command format (frontmatter + body in `.claude/commands/`)
  - MCP configuration in `~/.claude/settings.json` or `.claude/settings.json`
- Do not modify files outside `04-tooling/` during these exercises.
- Each exercise has its own scoped CLAUDE.md — read it before starting.
