# CLAUDE.md — 05-workflow-automation

- Module: 05-workflow-automation
- Purpose: capstone — automate a real recurring task using Model, Context, Tools, and Prompts together
- Available tools: Bash, Read, Write
- Key concepts:
  - the automation target is a real task from the participant's own work
  - a prompt + bash wrapper is the minimal viable automation
  - a CLAUDE.md scopes the automation's context — it constrains what Claude can see and do
  - a programmable pipeline separates prompt.md (the spec) from the runtime (bash/python/ts)
- Do not modify files outside 05-workflow-automation/ during these exercises.
- Each exercise has its own scoped CLAUDE.md — read it before starting.
