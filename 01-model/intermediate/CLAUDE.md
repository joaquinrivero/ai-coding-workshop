# CLAUDE.md — 01-model intermediate

- Exercise: 01-model intermediate — model routing heuristic
- Allowed tools: Bash (read-only: `claude --model --print`), Read, Write (routing-prompt.md only)
- Focus: improve the classifier prompt in routing-prompt.md until it correctly routes all three test tasks
- The classifier should run on Haiku — the routing decision itself is a cheap task
- Do not modify README.md or CLAUDE.md
- Success criterion: all three test tasks receive the correct model recommendation with reasoning
